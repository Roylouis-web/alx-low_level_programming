/**
 * _isalpha - provides a similar function: isalpha.
 * @c:interger parameter for the below function
 * Return: Always Successful
 */
int _isalpha(int c)
{
if (c >= 'a' || c >= 'A')
{return (1);
}
return (0);
}
